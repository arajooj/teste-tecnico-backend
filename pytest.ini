[pytest]
pythonpath = .
testpaths = tests
addopts = -m "not stress"
markers =
    integration: local docker and infrastructure smoke tests
    stress: concurrent stress tests excluded from the default suite
